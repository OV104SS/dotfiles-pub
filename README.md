# dotfiles-pub

Public dotfiles shared across multiple machines.

## Setup

```bash
# Clone this repo, then create symlinks:
ln -snf /path/to/dotfiles-pub/tmux/.tmux.conf ~/.tmux.conf
```

## Contents

- `tmux/` - tmux configuration (requires tmux 3.2+)
